requests>=2.31.0
boto3>=1.35.0
python-dotenv>=1.0.0
pyarrow>=18.0.0

# Tests only. The image installs it too: it is small, and an exporter you
# cannot run the tests against on the host that runs it is one you will not
# test. See test_run.py.
pytest>=8.0.0
